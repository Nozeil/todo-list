import { createStyles } from '@mantine/styles';

export const useStyles = createStyles(() => ({
  root: { width: '100%' },
  labelWrapper: { width: '100%' },
  body: {
    width: '100%',
    alignItems: 'center',
  },
}));
